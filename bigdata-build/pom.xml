<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.leumanuel.aggregator</groupId>
    <artifactId>bigdata-build</artifactId>
    <version>1.0.9</version>
    <packaging>pom</packaging>

    <name>Big Data - Build Conventions</name>
    <description>Shared pluginManagement and profiles for the Big Data Platform.</description>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <jacoco.version>0.8.14</jacoco.version>
        <jasperreports.plugin.version>3.6.1</jasperreports.plugin.version>
        <exec.maven.plugin.version>3.6.3</exec.maven.plugin.version>

        <jacoco.line.minimum>0.90</jacoco.line.minimum>
        <jacoco.branch.minimum>0.90</jacoco.branch.minimum>

        <skip.perf>true</skip.perf>
        <skip.scan>true</skip.scan>
    </properties>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <configuration>
                        <argLine>@{argLine} -Xmx1g -XX:+EnableDynamicAgentLoading</argLine>
                    </configuration>
                </plugin>

                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${jacoco.version}</version>
                    <executions>
                        <execution>
                            <id>prepare-agent</id>
                            <goals><goal>prepare-agent</goal></goals>
                        </execution>
                        <execution>
                            <id>report</id>
                            <phase>verify</phase>
                            <goals><goal>report</goal></goals>
                        </execution>
                        <execution>
                            <id>check</id>
                            <phase>verify</phase>
                            <goals><goal>check</goal></goals>
                            <configuration>
                                <rules>
                                    <rule>
                                        <element>BUNDLE</element>
                                        <limits>
                                            <limit>
                                                <counter>LINE</counter>
                                                <value>COVEREDRATIO</value>
                                                <minimum>${jacoco.line.minimum}</minimum>
                                            </limit>
                                            <limit>
                                                <counter>BRANCH</counter>
                                                <value>COVEREDRATIO</value>
                                                <minimum>${jacoco.branch.minimum}</minimum>
                                            </limit>
                                        </limits>
                                    </rule>
                                </rules>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>

                <plugin>
                    <groupId>com.pro-crafting.tools</groupId>
                    <artifactId>jasperreports-plugin</artifactId>
                    <version>${jasperreports.plugin.version}</version>
                </plugin>

                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>exec-maven-plugin</artifactId>
                    <version>${exec.maven.plugin.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <profiles>
        <profile>
            <id>dev</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <env.name>dev</env.name>
                <liquibase.contexts>dev</liquibase.contexts>
            </properties>
        </profile>
        <profile>
            <id>staging</id>
            <properties>
                <env.name>staging</env.name>
                <liquibase.contexts>staging</liquibase.contexts>
            </properties>
        </profile>
        <profile>
            <id>prod</id>
            <properties>
                <env.name>prod</env.name>
                <liquibase.contexts>prod</liquibase.contexts>
            </properties>
        </profile>
        <profile>
            <id>perf</id>
            <properties><skip.perf>false</skip.perf></properties>
        </profile>
        <profile>
            <id>scan</id>
            <properties><skip.scan>false</skip.scan></properties>
        </profile>
    </profiles>

    <distributionManagement>
        <repository>
            <id>github</id>
            <name>GitHub Packages-bigdata-aggregator</name>
            <url>https://maven.pkg.github.com/Leupesquisa/bigdata-aggregator</url>
        </repository>
    </distributionManagement>
</project>